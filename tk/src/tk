#!/usr/bin/env python3
import argparse
import fire
from install.install import Install

class tk(object):
    def install(self, company, dev_env):
        Install().install(company_name=company, dev_env=dev_env)


if __name__ == '__main__':
  fire.Fire(tk)